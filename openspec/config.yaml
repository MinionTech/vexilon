schema: spec-driven

context: |
  Tech stack: Python 3.14 (experimental), Chainlit (Target UI), Ollama, Qwen 3
  
  CRITICAL CONSTRAINTS:
  - Python version MUST remain at 3.14. Downgrading to 3.12 or lower is strictly forbidden.
  - LLM Model target is Qwen 3. Do not downgrade to Qwen 2.5 or lower.
  
  Verification:
  - Check compose.yml, Containerfile, and pyproject.toml for version integrity.

# Per-artifact rules (optional)
# Add custom rules for specific artifacts.
# Example:
#   rules:
#     proposal:
#       - Keep proposals under 500 words
#       - Always include a "Non-goals" section
#     tasks:
#       - Break tasks into chunks of max 2 hours
